# USE CASES for further improvements

### Use Case 01: Store Animals as Dictionary Objects

**As a developer**
</br>**I want** to store each animal as a dictionary instead of a CSV string
</br>**So that** data is more structured, accessible, and easier to work with

### Use Case 02: Create Animal Dictionary from Validated Input

**As a system**
<br>**I want** to construct a dictionary object from validated user input
</br>**So that** new animals match the data structure of existing animals

### Use Case 03: Store Limb Count as Integer Type

**As a developer**
</br>**I want** limb count stored as an integer instead of a string
</br>**So that** numeric operations can be performed without type conversion

### Use Case 04: Eliminate CSV String Parsing Logic

**As a developer**
</br>**I want** to remove CSV parsing from the display function
</br>**So that** the code is simpler and less error-prone

### Use Case 05: Simplify Data Structure Comments

**As a developer**
</br>**I want** the code comments to reflect the new data structure
</br>**So that** documentation remains accurate

### Use Case 06: Enable Future Dictionary-Based Operations

**As a developer**
</br>**I want** animals stored as dictionaries
</br>**So that** future features can easily access and manipulate structured data